# 编译系统实践 — 项目要求与注意事项

## 一、任务概述

- **目标**：实现 ToyC 语言的编译器
- **流程**：ToyC 源文件 → 词法分析 → 语法分析 → 语义分析 → 代码生成 → RISC-V32 汇编代码
- **交付物**：
    1. 可正确执行的编译器（在线评测系统自动测试评分）
    2. 一份实践报告

---

## 二、实现语言与工具链

### 2.1 可选编程语言（三选一）

| 语言 | 版本要求 |
| --- | --- |
| C++ | C++20 |
| Java | JDK 21 |
| OCaml | 5.3.0 |

> **建议**：评测编译时间限制宽松，编译器自身执行效率不影响评分，选最擅长的语言即可。
> 

### 2.2 可用构建系统

| 语言 | 构建工具 |
| --- | --- |
| C++ | CMake 4.0.3、Make |
| Java | Maven 3.9.10、Gradle 8.5 |
| OCaml | Dune 3.19 |

> **注意**：项目中必须包含构建系统配置文件（`CMakeLists.txt`、`Makefile`、`pom.xml`、`build.gradle`、`dune-project` 等）。
> 

### 2.3 可用第三方库（词法/语法分析）

| 语言 | 可用库 |
| --- | --- |
| C++ | Flex、Bison/Yacc、ANTLR 4.13.1 |
| Java | ANTLR 4.13.1 |
| OCaml | ocamllex、ocamlyacc、Menhir |

### 2.4 禁止事项

- **禁止抄袭代码**
- **禁止套用 Clang、LLVM 等现成框架**

---

## 三、接口规范

### 3.1 输入输出

- **输入**：从 `stdin` 读取 ToyC 源程序代码
- **输出**：向 `stdout` 写入汇编程序代码
- **stderr**：输出随意，不影响评测

### 3.2 本地测试命令

```bash
/path/to/compiler < /path/to/input_file.tc > /path/to/output_file.s
```

### 3.3 优化参数

- 性能测试时编译器会接收 `opt` 参数表示开启优化
- 可选择在接受该参数时启用优化算法，也可忽略
- 鼓励实现多种优化算法：
    - 公共子表达式消除
    - 死代码删除
    - 寄存器分配优化
    - 其他提高汇编代码运行效率的优化

---

## 四、代码提交与评测

### 4.1 提交方式

将编译器项目上传到 Git 仓库（GitHub / Gitee / GitLab 均可）。

提交评测时需要提供：

- 仓库地址（含访问令牌）
- 分支名（可选）

示例格式：

```
https://myuser:ghp_xxxxxx@github.com/myuser/myproj.git
```

### 4.2 评测流程

评测系统自动完成以下步骤：

1. 克隆指定分支的代码到评测环境
2. 自动检测编程语言、项目结构、构建系统、可执行文件名或主类名
3. 进行构建和评测

---

## 五、评分标准

### 5.1 总分构成

```
总得分 = 评测得分 × 80% + 实践报告得分 × 20%
```

### 5.2 评测得分构成

```
评测得分 = 功能得分 × 75% + 性能得分 × 25%
```

### 5.3 功能得分

```
功能得分 = (通过的功能测试用例数量 / 功能测试用例总数量) × 100
```

### 5.4 性能得分

```
性能得分 = Σ min{1, 基准时间 / 实际运行时间} / 性能测试用例总数量 × 100
```

> 其中 `基准时间 = gcc -O2 生成代码的运行时间`，以满分封顶。
> 

### 5.5 时间限制

| 测试类型 | 单点时间限制 |
| --- | --- |
| 功能测试 | 1 秒 |
| 性能测试 | 20 秒 |

> **今年变化**：性能分数占比提高，鼓励后端优化。
> 

---

## 六、ToyC 语言定义

### 6.1 语言定位

ToyC 是 C 语言的简化子集，省去了以下特性：

- 数组
- 指针
- I/O
- 多文件编译

> ToyC 源文件可以用 C 编译器直接编译，运行结果与等同的 C 代码一致。
> 

### 6.2 文法（EBNF）

#### 程序结构

```
CompUnit → (Decl ∣ FuncDef)+
Decl     → ConstDecl ∣ VarDecl
ConstDecl → "const" "int" ID "=" Expr ";"
VarDecl  → "int" ID "=" Expr ";"
```

#### 函数定义

```
FuncDef → ("int" ∣ "void") ID "(" (Param ("," Param)*)? ")" Block
Param   → "int" ID
```

#### 语句

```
Stmt → Block
      ∣ ";"
      ∣ Expr ";"
      ∣ ID "=" Expr ";"
      ∣ Decl
      ∣ "if" "(" Expr ")" Stmt ("else" Stmt)?
      ∣ "while" "(" Expr ")" Stmt
      ∣ "break" ";"
      ∣ "continue" ";"
      ∣ "return" Expr? ";"

Block → "{" Stmt* "}"
```

#### 表达式（按优先级从低到高）

```
Expr      → LOrExpr
LOrExpr   → LAndExpr ∣ LOrExpr "||" LAndExpr
LAndExpr  → RelExpr ∣ LAndExpr "&&" RelExpr
RelExpr   → AddExpr ∣ RelExpr ("<"∣">"∣"<="∣">="∣"=="∣"!=") AddExpr
AddExpr   → MulExpr ∣ AddExpr ("+"∣"-") MulExpr
MulExpr   → UnaryExpr ∣ MulExpr ("*"∣"/"∣"%") UnaryExpr
UnaryExpr → PrimaryExpr ∣ ("+"∣"-"∣"!") UnaryExpr
PrimaryExpr → ID ∣ NUMBER ∣ "(" Expr ")" ∣ ID "(" (Expr ("," Expr)*)? ")"
```

### 6.3 终结符定义

| 终结符 | 规则 | 正则表达式 |
| --- | --- | --- |
| ID | C 标识符 | `[_A-Za-z][_A-Za-z0-9]*` |
| NUMBER | 十进制整数常量 | `-?(0\|[1-9][0-9]*)` |
- **空白字符**：空格、制表符、换行符等，忽略
- **注释**：与 C 语言一致
    - 单行注释：`//` 开始，到最近换行符前结束
    - 多行注释：`/*` 开始，以最近的 `/` 结束

---

## 七、ToyC 语义约束（重要）

### 7.1 程序结构

- 程序可包含：全局变量声明、全局常量声明、函数定义
- **必须**包含一个 `main` 函数：参数列表为空、返回类型为 `int`、作为入口点

### 7.2 函数规则

| 规则 | 说明 |
| --- | --- |
| 参数类型 | 仅支持 `int` 类型 |
| 返回类型 | `int` 或 `void` |
| 作用域 | 只能声明在全局作用域，名称唯一 |
| 声明位置 | 不能声明在函数体内 |
| 函数值 | 函数不能作为值储存、传递或运算 |
| 调用顺序 | 函数调用必须写在被调函数声明之后（可递归调用自身） |
| int 返回 | 每一条可能的执行路径上都必须通过 `return` 返回 `int` 值 |
| void 返回 | 可不包含 `return`；若包含则不能有返回值 |

### 7.3 语句规则

- 无返回值的函数调用表达式**不能**作为 `if`/`while` 条件或赋值语句的右值
- `break` 和 `continue` **只能**出现在循环中
- 其他语法和语义与 C 语言一致

### 7.4 变量与常量声明

#### 类型

- 仅支持 32 位有符号整数（`int`）

#### 初始化

- **所有声明（VarDecl 和 ConstDecl）必须带有初始化表达式**

#### 常量约束（const）

- 值在**编译期必须能确定**
- 初始化表达式只能包含：数字字面量、已声明的常量、以及由它们组合的算术/逻辑运算
- 常量**不能**作为赋值语句的左值（不可修改）

#### 作用域与生命周期

| 概念 | 说明 |
| --- | --- |
| 全局变量/常量 | 整个程序运行期间有效 |
| 局部变量/常量 | 从声明点开始，到所在作用域结束 |
| 函数形参 | 在函数体内可见 |
| Block 作用域 | 创建新的嵌套作用域，内层屏蔽外层同名标识符 |
| 声明顺序 | 变量和常量的使用必须发生在声明之后 |

### 7.5 表达式规则

- 运算符优先级、结合性和计算规则与 C 语言一致
- `&&` 和 `||` 遵循**短路计算**规则
- 非零视为"真"，零视为"假"
- 除数不能为零

---

## 八、测试用例说明

### 8.1 测试用例前提

- 所有测试用例的 ToyC 源文件**无语法错误**
- 符合该语言的**所有语义约束**
- **不包含未定义行为**
- 编译成的可执行文件能在**有限时间内执行完成**

### 8.2 通过判定

程序运行结果以**主函数的返回值**（即程序退出代码，0~255 之间的整数）为准。

通过条件：

1. 生成的代码在规定时间内完成运行
2. 运行结果与正确结果一致

---

## 九、开发注意事项清单

### 编译器前端

- [ ]  实现词法分析器：识别 ID、NUMBER、关键字、运算符、空白字符、注释
- [ ]  实现语法分析器：按 ToyC 文法构建 AST
- [ ]  实现语义分析：
    - [ ]  作用域管理（全局/局部/Block 嵌套）
    - [ ]  变量/常量声明检查（必须初始化、const 不可修改）
    - [ ]  函数声明与调用检查（参数类型、返回类型、调用顺序）
    - [ ]  break/continue 只能在循环中
    - [ ]  int 函数所有路径必须有 return
    - [ ]  void 函数 return 不能有返回值
    - [ ]  无返回值函数调用不能作为条件/右值
    - [ ]  短路计算语义

### 编译器后端

- [ ]  RISC-V32 汇编代码生成
- [ ]  函数调用约定（参数传递、返回值、栈帧管理）
- [ ]  控制流翻译（if-else、while、break、continue、return）
- [ ]  全局变量/常量的处理
- [ ]  常量折叠（编译期求值）

### 优化（可选，但强烈建议）

- [ ]  `opt` 参数处理
- [ ]  公共子表达式消除
- [ ]  死代码删除
- [ ]  寄存器分配优化
- [ ]  其他后端优化

### 工程规范

- [ ]  构建系统配置文件完整（CMakeLists.txt / Makefile / pom.xml 等）
- [ ]  stdin/stdout 接口正确
- [ ]  Git 仓库地址可访问（含访问令牌）
- [ ]  实践报告撰写

### 测试验证

- [ ]  功能测试：确保所有语义约束正确处理
- [ ]  性能测试：以 `gcc -O2` 为基准优化运行时间
- [ ]  边界情况：0~255 返回值、嵌套作用域、递归调用等

## 十、优化

### DAG进行局部优化

- **常量折叠 (Constant Folding) & 代数化简：**
    - **位置：** 基本块内部**构建 DAG 时**。
    - **做法：** 当发现 DAG 节点的左右子节点都是常数（如 `3 + 4`），或者符合代数定律（如 `x * 0`），直接不生成计算节点，而是返回一个常数节点 `7` 或 `0`。
- **公共子表达式消除 (CSE - Common Subexpression Elimination)：**
    - **位置：** 基本块内部**构建 DAG 时**。
    - **做法：** 利用 Hash-Consing，如果发现即将构建的 DAG 节点和已有的节点结构完全一样（比如又遇到了 `a + b`），直接将指针指向老的节点，不生成新节点。
- **常量传播 (Constant Propagation) & 复写传播 (Copy Propagation)：**
    - **位置：** 基本块内构建完 DAG 后，或者在基本块的三地址码列表中做正向扫描（可达定义分析）。
        - **路线 A（不用 DAG，直接正向扫描）：** 你根本不建 DAG，直接从上到下读基本块里的那串三地址码。你拿个小本本（环境表）记下来：读到 `x = 5`，记下 `x是5`；读到 `y = x`，查本子发现 `x是5`，就把这句改成 `y = 5`。这就是“正向扫描”。
        - **路线 B（利用 DAG）：** 在建 DAG 的时候，如果遇到 `y = x` 这样的复写，你干脆不给 `y` 建新节点，直接让 `y` 指向 `x` 的节点。等到最后把 DAG 展平回三地址码时，复写自然就没了。
    - **做法：** 维护一个环境表，记录当前哪个变量是常数，哪个变量是另一个变量的副本，用到时直接替换。
- **死代码消除 (DCE - Dead Code Elimination) (局部)：**
    - **位置：** 从 DAG **重新生成线性 IR 代码时**，或者逆向扫描基本块（活跃变量分析）。
    - **做法：** 如果 DAG 中的某个根节点（没有其他人使用它的值）不是函数的返回值，也没有修改内存等副作用，直接丢弃，不生成它的代码。
    
    **解释：** 死代码就是“算出来但没人用”的值。利用 DAG 做局部死代码消除，简直是**降维打击，自动完成**的。
    
    - **做法：** 假设你的基本块里有 `a = 1 + 2; b = 3 + 4; return a;`。
    - 建出来的 DAG 里有两个孤立的树：一个是计算 `a` 的，一个是计算 `b` 的。
    - 当你要把 DAG **展平（重新生成基本块内部的三地址码）时，你的算法会只从“有用的根节点”（比如要 return 的 `a`，或者要存入内存的值）开始往下拉**。
    - 因为 `b` 算出来之后没有任何有用的节点连着它，你的展平算法根本就不会遍历到 `b` 这个节点。
    - 结果就是：重新生成的代码里只有 `a = 3; return a;`，`b` 的代码因为没有被遍历到，就**自然而然地被丢弃了**。
- **强度折减 (Strength Reduction)：**
    - **位置：** 构建基本块的 DAG 或 IR 生成后（通常和代数化简一起做）。
    - **做法：** 比如遇到 `x * 2`，在 DAG 中直接替换为 `x + x` 或者左移指令 `x << 1`。

### 静态单赋值形式 (SSA - Static Single Assignment)

**作用位置：** 介于初始 CFG 生成之后，**全局优化（板书中的 `opt`）开始之前**。
• **做法：** 这是对整个 CFG 的一次大改造。通过插入 Φ (Phi) 节点和重命名变量（变成 $a_1, a_2$ 等），确保每个变量只被赋值一次。
• **意义：** 转成 SSA 后，数据流分析变得极其简单，后续的全局常量传播、全局 DCE 会像局部优化一样好写。

### 全局优化

**作用位置：** 对应板书图中的 **`CFG --opt--> CFG`** 阶段。此时需要跨越基本块边界，利用整个控制流图的信息。

- **全局常量/复写传播 & 全局 CSE & 全局 DCE：**
    - **位置：** 基于 SSA 形式的 CFG，或者在未转 SSA 的 CFG 上运行“数据流分析”（如到达定值分析、活跃变量分析）。
    - **做法：** 如果前一个基本块算出了 `x = 5`，且后续控制流一定经过这里且没被覆盖，就把下游所有块里的 `x` 都替换为 `5`。
- **代码移动 (Code Motion) & 循环优化 (Loop Optimization)：**
    - **位置：** CFG `opt` 阶段。
    - **做法：** 必须基于 CFG 进行“支配树 (Dominator Tree)”分析和“循环识别 (Loop Detection)”。例如**循环不变量外提 (LICM)**，如果在循环体内发现某些计算（如 `x = a + b`）在每次迭代都不变，就把它移动到循环前置节点（Preheader）中。

### 寄存器分配优化

**作用位置：** 对应板书图中的 **`IR <-- ra --> asm`** 阶段（最底部的 ra 指的就是 Register Allocation）。

- **位置：** 全局优化已经完成，IR 指令已经被选择为目标机器的低级 IR，在最后生成真实机器码（asm）之前。
- **做法：** 此时程序里用的是无限多的“虚拟寄存器”。通过“活跃区间分析 (Live Interval Analysis)”，构建寄存器冲突图，使用**图着色算法**或**线性扫描算法**，将虚拟寄存器映射到有限的物理寄存器上（并在不够用时插入 Spill 到内存的代码）。

描述：

程序通过lexer和parser获得AST，然后转变为IR为tac的CFG，CFG先进行局部优化（CFG的每个tac block转DAG，DAG转tac block替换原来的CFG里的block，实现局部优化），然后进行全局优化，可以先转SSA，然后进行全局优化；或者直接进行全局优化，获得优化后的CFG，CFG经过TCG（目标代码生成）得到riscv-like的代码，但是虚拟寄存器的IR，接着通过寄存器分配算法获得最终的riscv汇编代码。

局部优化：应用于单一基本块

- 强度折减：基本块DAG
- 常量折叠：基本块DAG
- 常量传播：基本块内构建完 DAG 后，或者在基本块的三地址码列表中做正向扫描
- 复写传播
- 死代码消除：从 DAG **重新生成线性 IR 代码时**，或者逆向扫描基本块
- 公共子表达式消除：基本块DAG
- 代数化简
- 静态单赋值形式：转变成另一种中间表示，每个变量有且仅被定义一次（a多次赋值，对应a0，a1，…）：介于初始 CFG 生成之后，**全局优化（板书中的 `opt`）开始之前**
- …

全局优化：应用于CFG

- 常量/复写传播
- 死代码消除
- 公共子表达式消除
- 代码移动
- 循环优化
- …

寄存器优化：IR到asm